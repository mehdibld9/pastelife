# Copy this file to `.env` and fill in the real keys.
# Do NOT commit your real `.env` (it's already in .gitignore).

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=public-anon-key-here
# Service role key is secret. Never commit it to a public repo.
SUPABASE_SERVICE_ROLE=service-role-key-here
